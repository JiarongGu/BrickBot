# Advanced Patterns

Non-automatable patterns that skills can't generate. Each entry: **Pattern**, **When to use**, **Implementation sketch**.

> Empty for now. Patterns get added here as they emerge during BrickBot development. Examples of what would belong here:
> - Lua sandbox setup (which globals to expose, which to deny)
> - Frame buffer locking strategy (single-writer, multi-reader)
> - Capture-target reacquisition when window resizes/minimizes
> - WinRT GraphicsCaptureItem disposal ordering
> - Cooperative cancellation through Lua coroutines
